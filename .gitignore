# OS noise
.DS_Store

# Dependencies
node_modules/

# Build output
dist/
out/
mobile/dist/
# typed-routes snapshot stays tracked so CI can typecheck without Metro
mobile/.expo/*
!mobile/.expo/types/
mobile/android/
mobile/ios/

# Secrets & server-local state — never in the repo
.env
*.env
!*.env.example
server/uploads/

# Archives / scratch
server.zip
*.log
scratch.js

# App binaries (EAS build downloads)
*.apk
*.aab
*.ipa

# web workspace build output
dist-web/

# admin console build output
dist-admin/

# local Claude settings
.claude/
mobile/fcm-service-account.json
*.aab
*firebase-adminsdk*.json
*.jks
